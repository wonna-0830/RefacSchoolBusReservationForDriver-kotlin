<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.refac_driverapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.refac_DriverApp"
        tools:targetApi="31">
        <activity
            android:name=".feature.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.refac_DriverApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.login.LoginActivity" />
        <activity android:name=".feature.register.RegisterActivity" />
        <activity android:name=".feature.clock.ClockActivity" />
        <activity android:name=".feature.routetime.RouteTimeActivity" />
        <activity android:name=".feature.finish.FinishActivity" />
        <activity android:name=".feature.selectbuslist.SelectBusListActivity" />
    </application>

</manifest>